# Лекция 3: Булевые значения, операторы сравнения и логика в JavaScript

## 1. Тип данных Boolean
**Boolean** — это базовый тип данных в JavaScript, который может принимать только два значения: `true` (истина) или `false` (ложь).

Тип данных Boolean появился в программировании благодаря **Алгебре Буля** — разделу математической логики, созданному английским математиком Джорджем Булем. Эта алгебра изучает высказывания, которые могут иметь только два значения (1 или 0). Эта система легла в основу работы всех компьютеров, электронных схем и условий в программировании.

В классической алгебре Буля существуют логические аксиомы, таблицы истинности и основные операции (конъюнкция, дизъюнкция, отрицание). Однако в JavaScript мы используем только практическую часть — логические значения и операторы, которые помогают управлять ходом программы.

**Для чего используются булевые значения:**
- создание логики программы
- создание условий (ветвлений)
- управление поведением программы (например, циклами)

### ✅ Когда появляется булево значение?
Булево значение возникает в четырёх основных случаях:

1. **При сравнении**
   Любое сравнение двух величин всегда возвращает `true` или `false`.
2. **В логических выражениях**
   Операторы `&&`, `||`, `!` приводят значения к булевому типу:
   ```javascript
   !true         // false
   true && false // false
   ```
3. **При проверке условий (`if`, `while`, тернарный оператор)**
   JS автоматически преобразует переданное значение к boolean:
   ```javascript
   if ("text") { ... } // "text" превращается в true
   if (0) { ... }      // 0 превращается в false
   ```
4. **При явном преобразовании**
   Через вызов функции `Boolean(value)`.

> 📌 **Итог:** Без типа `boolean` невозможно писать логику. Только благодаря ему код может реагировать на ситуацию, принимать решения и выполнять разные ветки инструкций.

---

## 2. Операторы сравнения
Операторы сравнения — это специальные символы, которые сравнивают два значения и возвращают результат в виде булевого значения (`true` или `false`). Без них невозможно написать условия, циклы или фильтрацию данных.

Сравнения в JS делятся на два важных типа:
1. Обычные сравнения величин (больше/меньше)
2. Сравнение на равенство (с тонкими нюансами)

### 2.1 Обычные сравнения
- `a > b` и `a < b` — строго больше / строго меньше
- `a >= b` и `a <= b` — больше или равно / меньше или равно

**Простые примеры:**
```javascript
console.log("10" > 2); // true (строка "10" превращается в число 10)
console.log("abc" > 2); // false (если один из операндов становится NaN, результат всегда false)
```

> ⚠️ **ВАЖНО: Строки сравниваются НЕ как числа!**
> Если оба операнда — строки, сравнение происходит посимвольно в алфавитном порядке на основе их **Unicode-кодов**.
> Unicode — это международный стандарт, который присваивает уникальный числовой код каждому символу (буквам, цифрам, эмодзи).
> ```javascript
> console.log("2" > "10"); // true (потому что "2" сравнивается с "1", а код "2" больше)
> console.log("a" > "b");  // false
> ```

**Как узнать Unicode-код символа:** `"строка".charCodeAt(index)`
```javascript
console.log("hello".charCodeAt(0)); // h -> 104
console.log("hello".charCodeAt(3)); // l -> 108
console.log("h" > "l");             // false (104 не больше 108)
```

**Примеры строковых сравнений:**
- `"яблоко" > "груша"` — сравнение идет посимвольно (первые буквы `я` и `г`). 
- Если первые буквы равны, сравниваются вторые. 
- Если все символы совпадают, но одна строка длиннее, то более длинная строка считается большей (`"кот" < "котик"` → `true`).

### 2.2 Сравнение на равенство
**Нестрогое равенство / неравенство (`==` и `!=`)**
Сравнивает значения с автоматическим приведением типов (если типы разные, JS пытается сделать их одинаковыми).
```javascript
5 == "5"          // true (строка становится числом)
true == 1         // true
false == 0        // true
"" == 0           // true
null == undefined // true (!!)
5 != "5"          // false
```
*Важно: Нестрогие равенства опасны непредсказуемым поведением, поэтому в современном JavaScript их используют редко.*

**Строгое равенство / неравенство (`===` и `!==`)**
Сравнивает и значения, и сами типы данных.
```javascript
5 === 5   // true
5 === "5" // false (разный тип данных)
5 !== 5   // false
5 !== "5" // true
```
> 🟦 **Правило:** Используйте только строгие операторы (`===` и `!==`). В профессиональном коде они применяются в 99% случаев.

---

## 3. Условия (`if` / `else if` / `else`)
Условия — это конструкция, которая позволяет выполнять определённый участок кода, только если заданное выражение истинно (`true`). Если условие ложное (`false`), блок кода пропускается.

### 3.1 Базовая форма: `if`
```javascript
if (age >= 18) {
    console.log("Вход разрешён");
}
```

### 3.2 Ветвление: `if ... else`
```javascript
if (password === "1234") {
    console.log("Пароль верный");
} else {
    console.log("Пароль неверный");
}
```

### 3.3 Множественные проверки: `if ... else if ... else`
Используется, когда нужно проверить несколько разных условий по очереди.
```javascript
if (score >= 90) {
    console.log("Отлично");
} else if (score >= 70) {
    console.log("Хорошо");
} else if (score >= 50) {
    console.log("Удовлетворительно");
} else {
    console.log("Плохо");
}
```

### 3.4 Вложенные условия
Нужны, когда решение зависит от нескольких последовательных проверок.
```javascript
let age = 20;
let country = "CZ";

if (age >= 18) {
    if (country === "CZ") {
        console.log("Доступ разрешён (возраст + страна)");
    } else {
        console.log("Возраст ок, но страна не подходит");
    }
} else {
    console.log("Недостаточно лет");
}
```

---

## 4. Логические операторы
Чтобы избежать огромного количества вложенных условий, используют логические операторы. Они объединяют или инвертируют условия. Результатом всегда будет `boolean`.

### 4.1 `&&` (AND — И)
Возвращает `true`, только если **оба** условия истинны. Если хотя бы одно `false` — результат `false`.
```javascript
if (age >= 18 && hasTicket) {
    console.log("Можно войти");
} else {
    console.log("Доступ запрещён");
}
```

### 4.2 `||` (OR — ИЛИ)
Возвращает `true`, если **хотя бы одно** условие истинно.
```javascript
let day = "Суббота";
if (day === "Суббота" || day === "Воскресенье") {
    console.log("Выходной");
}
```

### 4.3 `!` (NOT — НЕ)
Инвертирует логическое значение (`true` → `false`, `false` → `true`).
```javascript
let isRaining = false;
if (!isRaining) {
    console.log("Можно идти гулять");
}
```

### 4.4 Комбинирование логических операторов
Можно сочетать несколько операторов в одном условии (используйте скобки для приоритета):
```javascript
// Войти можно, если (взрослый + есть билет) ИЛИ (есть VIP-пас)
if ((age >= 18 && hasTicket) || vipPass) {
    console.log("Доступ разрешён");
}
```

### 4.5 Приемы для упрощения кода
Можно заменить короткий `if` на запись в одну строку:
```javascript
// Вместо этого:
if (age >= 18) {
    if (hasTicket) {
        console.log("OK");
    }
}

// Можно писать так:
if (age >= 18 && hasTicket) console.log("OK");
```

---

## 5. Тернарный оператор
Тернарный оператор — это короткая запись простого условия `if...else` в одну строку.

**Синтаксис:**
`условие ? значение_если_true : значение_если_false`

**Пример:**
```javascript
let age = 18;
// Вместо 5 строк кода if...else:
let result = age >= 18 ? "Можно войти" : "Доступ запрещён";
console.log(result);
```

**Особенности тернарного оператора:**
1. Можно сразу выводить в консоль: 
   `console.log(age >= 18 ? "Adult" : "Child");`
2. Удобно использовать для динамического HTML (в шаблонных строках):
   ```javascript
   document.body.innerHTML = `<h1>${age >= 18 ? "Добро пожаловать" : "Вход запрещён"}</h1>`;
   ```
3. **Не используйте**, если логика слишком сложная (много вложенностей), так как код станет нечитаемым. Идеально подходит для простых условий и присвоения значений.

---

## 💻 Практические задания

**1. Проверка возраста**
Просите пользователя ввести свой возраст, если ему больше или равано 18 выводим сообщение, что допуск разрещён
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
// запрашиваем возраст у пользователя и сразу переводим в number 
let age = +prompt("Введите возраст");

// проверяем: если возраст больше или равен 18
if (age >= 18) {
    // выводим сообщение о допуске
    console.log("Доступ разрешён");
} else {
    // иначе доступ запрещён
    console.log("Доступ запрещён");
}
```

</details>

**2. Проверка чётности числа**
Пользователь вводит число и мы проверяем его чётное оно или нет
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
// ввод числа
let num = +prompt("Введите число");

// если остаток от деления на 2 равен 0 → число чётное
if (num % 2 === 0) {
    console.log("Чётное");
} else {
    // иначе нечётное
    console.log("Нечётное");
}
```

</details>

**3. Больше / меньше / равно**
 Сравниваем два числа которые пользователь вводит с клавиатуры
 <details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let a = +prompt("Введите первое число");
let b = +prompt("Введите второе число");

// сравнение чисел
if (a > b) {
    console.log("Первое больше");
} else if (a < b) {
    console.log("Второе больше");
} else {
    // если равны
    console.log("Равны");
}
```

</details>

**4. Проверка логина и пароля**
- обьявляем две переменные которых зраняии в себе логин и пароль;
- просим пользователя по очереди ввести логин и пароль, если всё совпадает то вход разрещён
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
// правильные логин и пароль
let correctLogin = "admin";
let correctPassword = "1234";

// ввод пользователя
let login = prompt("Введите логин");
let password = prompt("Введите пароль");

// проверка совпадения
if (login === correctLogin && password === correctPassword) {
    console.log("Вход разрешён");
} else {
    console.log("Ошибка входа");
}
```

</details>

**5. Определение сезона**
Пользователь вводит номер месяйа от 1 до 12 и программа выводит ему название сезона в соответсвии к какому сезону относиться этот месяц
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let month = +prompt("Введите номер месяца (1-12)");

// проверяем диапазоны месяцев
if (month === 12 || month === 1 || month === 2) {
    console.log("Зима");
} else if (month >= 3 && month <= 5) {
    console.log("Весна");
} else if (month >= 6 && month <= 8) {
    console.log("Лето");
} else if (month >= 9 && month <= 11) {
    console.log("Осень");
} else {
    console.log("Ошибка");
}
```

</details>

**6. Делится ли число на 3 и 5 одновременно**
Проверяем делиться ли введённое число пользователем одновременно на 3 и на 5
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let num = +prompt("Введите число");

// проверяем делимость сразу на 3 и 5
if (num % 3 === 0 && num % 5 === 0) {
    console.log("Делится на 3 и 5");
} else {
    console.log("Не делится");
}
```

</details>

**7. Проверка длины строки**
Проверяем строку и віводим сообщение короткая если количество символов меньше 5, нормальная - от 5 до 15 и длинная - больше 15
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let str = prompt("Введите строку");

// проверяем длину строки
if (str.length < 5) {
    console.log("Короткая");
} else if (str.length <= 15) {
    console.log("Нормальная");
} else {
    console.log("Длинная");
}
```

</details>

**8. Проверка диапазона (от 10 до 20)**
Проверка лежит ли введённок число в диапазоне от 10 до 20
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let num = +prompt("Введите число");

// проверка диапазона
if (num >= 10 && num <= 20) {
    console.log("В диапазоне");
} else {
    console.log("Вне диапазона");
}
```

</details>

**9. Вложенные условия: кинотеатр**
Попросите пользователя ввести возраст.
- Если возраст < 16 → доступ запрещён.
- Если возраст от 16 до 18 → доступ только с родителями.
- Если 18 и больше → доступ разрешён.
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let age = +prompt("Введите возраст");

// проверка возраста
if (age < 16) {
    console.log("Доступ запрещён");
} else if (age < 18) {
    console.log("Только с родителями");
} else {
    console.log("Доступ разрешён");
}
```
</details>

**10. Погода и температура**
Пользователь вводит погоду (`sunny`, `cloudy`, `rain`) и температуру.
- Если (`sunny` или `cloudy`) И температура выше 15 — вывести «Можно гулять».
- Иначе — «Лучше остаться дома».
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let weather = prompt("Введите погоду (sunny, cloudy, rain)");
let temp = +prompt("Введите температуру");

// если погода хорошая И температура выше 15
if ((weather === "sunny" || weather === "cloudy") && temp > 15) {
    console.log("Можно гулять");
} else {
    console.log("Лучше остаться дома");
}
```
</details>

**11. Проверка пробела в строке**
Пользователь вводит текст.
- Если строка содержит пробел — вывести «Введено несколько слов».
- Иначе — «Одно слово».
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let text = prompt("Введите текст");

// проверяем наличие пробела
if (text.includes(" ")) {
    console.log("Несколько слов");
} else {
    console.log("Одно слово");
}
```
</details>

**12. Тернарный оператор: скидка**
Пользователь вводит сумму покупки.
- Если сумма больше 1000 → скидка 10%, иначе 0%.
- Вывести итоговую сумму (обязательно использовать тернарный оператор).
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let sum = +prompt("Введите сумму");

// определяем скидку через тернарный оператор
let discount = sum > 1000 ? 0.1 : 0;

// считаем итоговую сумму
let total = sum - sum * discount;

// вывод результата
console.log(total);
```
</details>

**13. Оценка по баллам**
Пользователь вводит число от 0 до 100. Вывести:
- 90–100 → A
- 70–89 → B
- 50–69 → C
- меньше 50 → F
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let score = +prompt("Введите баллы");

// проверка диапазонов
if (score >= 90) {
    console.log("A");
} else if (score >= 70) {
    console.log("B");
} else if (score >= 50) {
    console.log("C");
} else {
    console.log("F");
}
```

</details>

**14. Форматирование имени**
Пользователь вводит имя.
- Если имя начинается с заглавной буквы — оставить как есть.
- Если с маленькой — программно это исправить.
- Вывести исправленное имя на экран.
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let name = prompt("Введите имя");

// берём первую букву
let first = name[0];

// если первая буква маленькая
if (first === first.toLowerCase()) {
    // делаем её заглавной
    name = first.toUpperCase() + name.slice(1);
}

// выводим имя
console.log(name);
```
</details>

**15. Продвинутый тернарный оператор**
Пользователь вводит возраст и видит результат (использовать цепочку тернарных операторов):
- до 12 — ребёнок
- до 18 — подросток
- до 60 — взрослый
- старше 60 — пенсионер

<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let age = +prompt("Введите возраст");

// цепочка тернарных операторов
let result = age < 12 ? "Ребёнок" :
             age < 18 ? "Подросток" :
             age < 60 ? "Взрослый" :
             "Пенсионер";

// вывод результата
console.log(result);
```
</details>

**16. Строка в число с проверкой на NaN**
Дана строка, которую нужно перевести в число. Если строка не перевелась (результат `NaN`) — вернуть `null`. 
*(Подсказка: вспомните прошлую лекцию — почему нельзя использовать обычное сравнение с `NaN`, и примените правильный метод проверки).*
<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
let value = prompt("Введите число");

// преобразуем строку в число
let num = Number(value);

// проверяем на NaN (правильный способ)
if (isNaN(num)) {
    // если не число → возвращаем null
    console.log(null);
} else {
    // иначе выводим число
    console.log(num);
}
```
</details>

**⭐17. Умная проверка доступа в систему**
Пользователь вводит: логин, пароль, возраст. Если логин или пароль неверные → ❌ «Неверные данные». Если логин и пароль верные: проверяем возраст < 16, если возраст не верен → ❌ «Слишком рано»; если возраст от 16 до 18 → ⚠️ «Ограниченный доступ»; если 18+ → ✅ «Полный доступ».

*Дополнительно:*
если логин `= "admin"`→ всегда **«Полный доступ»** (даже если возраст маленький)

<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
// правильные данные
let correctLogin = "user";
let correctPassword = "1234";

// ввод пользователя
let login = prompt("Введите логин");
let password = prompt("Введите пароль");
let age = +prompt("Введите возраст");

// сначала проверяем логин и пароль
if (login === correctLogin && password === correctPassword) {

    // отдельная проверка для админа
    if (login === "admin") {
        console.log("Полный доступ");
    } else {

        // проверка возраста
        if (age < 16) {
            console.log("Слишком рано");
        } else if (age < 18) {
            console.log("Ограниченный доступ");
        } else {
            console.log("Полный доступ");
        }

    }

} else {
    // если логин или пароль неверные
    console.log("Неверные данные");
}
```
</details>

**⭐18. Система входа с безопасностью и бонусами**
Пользователь вводит: логин, пароль, возраст, сумму на счету.
Правила:
1. Проверка логина и пароля: если неверно → ❌ «Доступ запрещён»;
2. Если всё верно, проверка или особый пользователь, то есть если логин `= "admin"` → ✅ «Полный доступ + все функции»;
3. Для обычных пользователей проверяем возраст:
         - < 16 → ❌ «Доступ запрещён»;
         - 16–17 → ⚠️ «Ограниченный доступ»;
         - 18+ → идём дальше
5. Баланс, если баланс < 100 → «Минимальный доступ»; 100–1000 → «Стандартный доступ»; 1000 → «VIP доступ»
6. Бонус, если: возраст ≥ 18 и баланс > 500 ->  «+ Бонус активирован»

<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
// правильные данные
let correctLogin = "user";
let correctPassword = "1234";

// ввод данных
let login = prompt("Введите логин");
let password = prompt("Введите пароль");
let age = +prompt("Введите возраст");
let balance = +prompt("Введите баланс");

// проверка логина и пароля
if (login === correctLogin && password === correctPassword) {

    // проверка на администратора
    if (login === "admin") {
        console.log("Полный доступ + все функции");
    } else {

        // проверка возраста
        if (age < 16) {
            console.log("Доступ запрещён");
        } else if (age < 18) {
            console.log("Ограниченный доступ");
        } else {

            // переменная для уровня доступа
            let access = "";

            // проверка баланса
            if (balance < 100) {
                access = "Минимальный доступ";
            } else if (balance <= 1000) {
                access = "Стандартный доступ";
            } else {
                access = "VIP доступ";
            }

            // проверка бонуса
            if (age >= 18 && balance > 500) {
                access = access + " + Бонус активирован";
            }

            // вывод результата
            console.log(access);
        }

    }

} else {
    console.log("Доступ запрещён");
}
```
</details>

**⭐19. Регистрация + проверка сложности пароля + вход**
Пользователь проходит систему из нескольких этапов:
* **1. Регистрация**
Пользователь вводит: логин, пароль. Пароль считается **валидным**, если:
- длина пароля **8 символов или больше**
- содержит **хотя бы одну цифру**
- содержит **хотя бы одну заглавную букву**
- содержит **хотя бы один спецсимвол** (`!@#$%^&*`)
Если пароль не проходит проверку → вывести:
- ❌ «Слабый пароль»
* **2. Вход в систему**
Если регистрация успешна:
пользователь снова вводит:
- логин  
- пароль. Проверка входа: если логин или пароль неверный → ❌ «Ошибка входа»; если данные совпадают → перейти дальше.
* **3. Проверка возраста**
Пользователь вводит возраст:
- если возраст < 16 → ❌ «Нет доступа»
- если возраст от 16 до 17 → ⚠️ «Ограниченный доступ»
- если возраст 18 и больше → перейти дальше
* **4. Секретная функция**
Если логин содержит слово `"admin"` →  
вывести:  
- 🛠️ «Режим разработчика»
иначе:
- ✅ «Полный доступ»

<details>
  <summary>💡 Посмотреть решение</summary>

```javascript
// ===== ЭТАП 1: РЕГИСТРАЦИЯ =====

// ввод логина
let regLogin = prompt("Придумайте логин");

// ввод пароля
let regPassword = prompt("Придумайте пароль");

// проверяем длину пароля (минимум 8 символов)
let hasLength = regPassword.length >= 8;

// проверяем наличие цифры
let hasNumber = regPassword.includes("0") || regPassword.includes("1") ||
                regPassword.includes("2") || regPassword.includes("3") ||
                regPassword.includes("4") || regPassword.includes("5") ||
                regPassword.includes("6") || regPassword.includes("7") ||
                regPassword.includes("8") || regPassword.includes("9");

// проверяем наличие заглавной буквы
let hasUpper = regPassword !== regPassword.toLowerCase();

// проверяем спецсимволы
let hasSpecial = regPassword.includes("!") || regPassword.includes("@") ||
                 regPassword.includes("#") || regPassword.includes("$") ||
                 regPassword.includes("%") || regPassword.includes("^") ||
                 regPassword.includes("&") || regPassword.includes("*");

// если все условия выполнены
if (hasLength && hasNumber && hasUpper && hasSpecial) {

    console.log("Регистрация успешна");

    // ===== ЭТАП 2: ВХОД =====

    let login = prompt("Введите логин");
    let password = prompt("Введите пароль");

    // проверяем совпадение
    if (login === regLogin && password === regPassword) {

        // ===== ЭТАП 3: ВОЗРАСТ =====

        let age = +prompt("Введите возраст");

        if (age < 16) {
            console.log("Нет доступа");
        } else if (age < 18) {
            console.log("Ограниченный доступ");
        } else {

            // ===== ЭТАП 4: БОНУС =====

            // проверяем наличие слова admin в логине
            if (login.includes("admin")) {
                console.log("Режим разработчика");
            } else {
                console.log("Полный доступ");
            }

        }

    } else {
        console.log("Ошибка входа");
    }

} else {
    // если пароль слабый
    console.log("Слабый пароль");
}
```
</details>

### 🛠 Полезные ссылки к Лекции 3

* **[Учебник learn.javascript.ru: Операторы сравнения](https://learn.javascript.ru/comparison)** — всё о том, как сравниваются строки, числа и разные типы данных. Обязательно к прочтению, чтобы понять разницу между `==` и `===`.
* **[Учебник learn.javascript.ru: Условное ветвление](https://learn.javascript.ru/ifelse)** — подробная статья про `if`, `else` и использование тернарного оператора `?`.
* **[Учебник learn.javascript.ru: Логические операторы](https://learn.javascript.ru/logical-operators)** — глубокий разбор того, как работают `&&`, `||` и `!`, а также их особенности в JavaScript (например, возвращение первого истинного или ложного значения).
* **[MDN Web Docs: if...else](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/if...else)** — официальная документация по условным инструкциям с хорошими примерами.
* **[MDN Web Docs: Условный (тернарный) оператор](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Operators/Conditional_Operator)** — короткая и понятная справка по синтаксису тернарного оператора.

---

### 🗺 Навигация:

[⬅ Лекция 2: Типы данных числа и строки](./lekce2.md) | [🏠 В оглавление](./README.md) | [Лекция 4: Управление потоком: Switch, циклы for/while ➡](./lekce4.md)
