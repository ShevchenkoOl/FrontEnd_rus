## lekce 1: Ввод в JavaScript, переменный, вывод результата, типы данных, взаемодействие с пользователем, арифметические операторы
## lekce 2: Числа и строки в JavaScript, перевод типов, десятичные числа, NaN, undefined, методы Math, свойства и методы строк
## lekce 3: Boolean, операторы сравнение, логические операторы, условие if…else, тернарный оператор
## lekce 4: Switch, циклы for и while
## lekce 5: Массивы, объекты и их методы в JavaScript
## lekce 6: Функции в JavaScript (объявление функции, функциональное выражение, анонимные и стрелочные функции)
## lekce 7: Callback-функции. Методы массивов с callback-функциями: forEach, map, filter, find, findIndex, some, every, reduce, sort, setTimeout(), setInterval().
## lekce 8: DOM (Document Object Model), получение елементов, свойста (style, classList) и атртбуты, создание, изменение и добавление елементов.
## lekce 9: События, event.target и event.currentTarget, submit, input, change, input.value 
## lekce 10: Веб хранилища (LocalStorage SessionStorage), JSON-формат
## lekce 11:	Асинхронность в JavaScript
## lekce 12:	Promises + async/await
## lekce 13:	Модули ES6 (import/export)
## lekce 14:	Vite (установка, сборка, структура)
## lekce 15:	Свой API + JSON Server + ООП + тестирование


# Завершающий проект по JavaScript
## Вариант 1 - форма поиска изображений:
### Требования к функциональности:
1. Форма поиска изображений — текстовое поле для ввода запроса; после отправки формы выполняется поиск и отображаются результаты.
2. Отображение результатов — изображения отображаются в виде галереи (сетки), каждое изображение кликабельно и при нажатии открывается в модальном окне с подробной информацией: автор, теги, количество загрузок, количество лайков.
3. Пагинация — отображение нескольких страниц результатов с помощью кнопок «Предыдущая» и «Следующая», информация о текущей странице (например, «Страница 1 из 5»).
4. Анимация загрузки (спиннер) — отображается при загрузке данных из API.
5. Закрытие модального окна — нажатием на крестик, щелчком вне изображения, нажатием клавиши Escape.
6. Дополнительные задачи (необязательно, но рекомендуется):
⭐️ Избранные изображения — каждое изображение имеет кнопку «★», которую можно использовать для добавления его в избранное; избранные изображения можно просмотреть, нажав кнопку «Просмотреть избранное», классическая пагинация в избранном не отображается.
📱 Адаптивный дизайн — приложение должно хорошо работать на разных устройствах (например, мобильных телефонах, планшетах).

Документация API Pixabay:   

## Вариант 2: Апгрейд HTML & CSS проекта с использованием JavaScript
Доработать свой существующий HTML & CSS проект, добавив в него логику и интерактивность на JavaScript.
Обязательные требования (JavaScript):
1️. Работа с DOM. Проект должен использовать JavaScript для:
    - получения элементов страницы (querySelector, querySelectorAll);
    - изменения контента (textContent, innerHTML);
    - изменения классов и стилей (classList, style);
    - создания и удаления элементов (createElement, append, remove).
        Пример:
            динамически добавлять карточки, пункты меню, отзывы, проекты и т.д.
2️. События. Использовать addEventListener минимум для 3 типов событий:
    - click
    - submit или input
    - Дополнительно (на выбор): keydown, change, scroll, mouseover / mouseout
        Пример:
            - открытие / закрытие модального окна;
            - переключение активных элементов;
            - интерактивные кнопки.
3️. Формы + валидация. Если в проекте есть форма (контакты, подписка, логин и т.д.):
    - отмена стандартного поведения (preventDefault);
    - чтение значений полей (input.value);
    - базовая валидация:
    - пустые поля,
    - минимальная длина,
    - корректный email.

    Дополнительно:
        - вывод сообщений об ошибках под полями;
        - подсветка ошибок через классы.
4️. Модальное окно (через JS):
    - открытие и закрытие через JavaScript;
    - закрытие: по кнопке ✖, по клику вне окна, по клавише Escape.
5️. Работа с данными (массивы и объекты). В проекте должна быть логика с:
    - массивами объектов;
    - перебором (forEach, map, filter — минимум один метод).
        Пример: 
            - массив карточек (проекты, товары, услуги);
            - фильтрация или поиск по элементам;
            - динамическая генерация HTML из массива.
6. LocalStorage. Использовать localStorage минимум в одном месте:
Возможные варианты:
- сохранение темы (dark / light);
- сохранение данных формы;
- избранные элементы;
- состояние интерфейса (открыто/закрыто).
7. - Loader / spinner при действии пользователя
⭐ Дополнительные задания (по желанию). Можно выбрать 1–2 пункта:
    - Переключение темы (light / dark)
    - Фильтрация или поиск по элементам
    - Пагинация или «показать ещё»
    - Управление с клавиатуры
    - Разделение кода на модули (type="module")
    - Требования к коду